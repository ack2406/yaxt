{
    "swagger": "2.0",
    "info": {
      "version": "1.0.0",
      "title": "YAXT API"
    },
    "host": "localhost:5000",
    "basePath": "/api",
    "schemes": [
      "http"
    ],
    "paths": {
      "/tests": {
        "get": {
          "summary": "Get all tests",
          "description": "Returns a list of all tests",
          "produces": [
            "application/json"
          ],
          "responses": {
            "200": {
              "description": "Success",
              "schema": {
                "type": "array",
                "items": {
                  "$ref": "#/definitions/Test"
                }
              }
            }
          }
        },
        "post": {
          "summary": "Create a new test",
          "description": "Create a new test and return it",
          "consumes": [
            "application/json"
          ],
          "produces": [
            "application/json"
          ],
          "parameters": [
            {
              "in": "body",
              "name": "test",
              "description": "The test to create",
              "required": true,
              "schema": {
                "$ref": "#/definitions/Test"
              }
            }
          ],
          "responses": {
            "201": {
              "description": "Success",
              "schema": {
                "$ref": "#/definitions/Test"
              }
            }
          }
        }
      },
      "/questions": {
        "get": {
          "summary": "Get all questions",
          "description": "Returns a list of all questions",
          "produces": [
            "application/json"
          ],
          "responses": {
            "200": {
              "description": "Success",
              "schema": {
                "type": "array",
                "items": {
                  "$ref": "#/definitions/Question"
                }
              }
            }
          }
        },
        "post": {
          "summary": "Create a new question",
          "description": "Create a new question and return it",
          "consumes": [
            "application/json"
          ],
          "produces": [
            "application/json"
          ],
          "parameters": [
            {
              "in": "body",
              "name": "question",
              "description": "The question to create",
              "required": true,
              "schema": {
                "$ref": "#/definitions/Question"
              }
            }
          ],
          "responses": {
            "201": {
              "description": "Success",
              "schema": {
                "$ref": "#/definitions/Question"
              }
            }
          }
        }
      }
    },
    "definitions": {
      "Test": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string"
          },
          "description": {
            "type": "string"
          }
        }
      }
    }
  }